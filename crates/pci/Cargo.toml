[package]
name = "kernel-pci"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
name = "kernel_pci"
path = "src/lib.rs"

[dependencies]
kernel-sync = { path = "../sync" }
